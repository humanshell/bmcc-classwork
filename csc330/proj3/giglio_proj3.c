#include <stdio.h>
#include <string.h>
#include "gstack.h"

// main function
int main(void) {

  // free stacks, expression and return
  //free(operator_stack);
  //free(number_stack);
  //free(expression);
  return 0;

}

